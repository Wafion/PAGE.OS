// This page has been deprecated. PDFs are now opened in a new tab directly from search results.
// This prevents issues with websites blocking server-side fetching of PDF files.
export default function DeprecatedPdfReaderPage() {
  return null;
}
